<%= form_with(model: loginuser, local: true) do |form| %>
  <% if loginuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loginuser.errors.count, "error") %> prohibited this loginuser from being saved:</h2>

      <ul>
      <% loginuser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :loginuser_name %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, id: :loginuser_password %>
  </div>

  <div class="field">
    <%= form.label :password_confirmation %>
    <%= form.password_field :password_confirmation, id: :loginuser_password_confirmation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
